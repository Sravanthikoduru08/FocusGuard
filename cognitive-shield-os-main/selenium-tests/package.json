{
  "name": "focusguard-selenium-tests",
  "version": "1.0.0",
  "description": "Selenium E2E Tests for FocusGuard Web App",
  "scripts": {
    "test": "mocha tests/**/*.test.js --timeout 30000",
    "login": "mocha tests/login.test.js --timeout 30000"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "selenium-webdriver": "^4.10.0"
  }
}
