name: FocusGuard CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  website-e2e-tests:
    name: Website E2E (${{ matrix.browser }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [chrome, firefox, edge]

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache-dependency-path: 'web-app/selenium-tests/package-lock.json'

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install openpyxl

    - name: Install Microsoft Edge
      if: matrix.browser == 'edge'
      run: |
        sudo apt-get update
        sudo apt-get install -y software-properties-common apt-transport-https wget curl
        curl https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > microsoft.gpg
        sudo install -o root -g root -m 644 microsoft.gpg /etc/apt/trusted.gpg.d/
        sudo sh -c 'echo "deb [arch=amd64] https://packages.microsoft.com/repos/edge stable main" > /etc/apt/sources.list.d/microsoft-edge-dev.list'
        rm microsoft.gpg
        sudo apt-get update
        sudo apt-get install -y microsoft-edge-stable

    - name: Install App Dependencies
      run: |
        cd web-app
        npm install

    - name: Install Testing Dependencies
      run: |
        cd web-app/selenium-tests
        npm ci || npm install

    - name: Start Development Server
      run: |
        cd web-app
        npm run dev -- --port 5173 &
        sleep 12

    - name: Run Selenium E2E Tests
      env:
        CI: true
        BROWSER: ${{ matrix.browser }}
      run: |
        cd web-app/selenium-tests
        npx mocha tests/login.test.js --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=report 2>&1 | tee mocha-run.log

    - name: Generate Excel Report
      if: always()
      run: |
        python scripts/generate_selenium_excel_report.py web-app/selenium-tests/mochawesome-report/report.json ${{ matrix.browser }} web-app/selenium-tests/mochawesome-report/report.xlsx

    - name: Prepare Website E2E Artifacts
      if: always()
      run: |
        mkdir -p website-artifacts
        cp -r web-app/selenium-tests/mochawesome-report/* website-artifacts/ || true
        cp web-app/selenium-tests/mocha-run.log website-artifacts/ || true
        cp *.png website-artifacts/ || true
        cp web-app/selenium-tests/*.png website-artifacts/ || true
        ls -R website-artifacts

    - name: Upload Website E2E Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: website-e2e-${{ matrix.browser }}-artifacts
        path: website-artifacts/ 
        if-no-files-found: warn

  android-build-and-test:
    name: Android Build & QA
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Configure Gradle JVM for Linux
      run: |
        sed -i '/org.gradle.java.home/d' android-app/gradle.properties
        sed -i '/org.gradle.java.installations/d' android-app/gradle.properties

    - name: Setup Java JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'zulu'

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install openpyxl

    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2

    - name: Grant Execute Permission for Gradlew
      run: chmod +x android-app/gradlew

    - name: Run Android Lint Checks
      run: cd android-app && ./gradlew lintDebug

    - name: Run JUnit Tests
      run: cd android-app && ./gradlew testDebugUnitTest

    - name: Run Code Security Checks
      run: |
        python scripts/android_security_scanner.py
        python scripts/generate_report.py

    - name: Build Debug APK
      run: cd android-app && ./gradlew assembleDebug

    - name: Prepare Android Artifacts
      if: always()
      run: |
        mkdir -p android-artifacts/apk
        mkdir -p android-artifacts/lint-reports
        mkdir -p android-artifacts/junit-reports
        mkdir -p android-artifacts/security-reports
        
        # Copy APK
        cp android-app/app/build/outputs/apk/debug/app-debug.apk android-artifacts/apk/ || true
        
        # Copy Lint reports
        cp android-app/app/build/reports/lint-results-debug.html android-artifacts/lint-reports/ || true
        cp android-app/app/build/reports/lint-results-debug.xml android-artifacts/lint-reports/ || true
        
        # Copy JUnit reports
        cp -r android-app/app/build/reports/tests/testDebugUnitTest/* android-artifacts/junit-reports/ || true
        
        # Copy Security reports
        cp reports/security/FocusGuard_Security_Assessment.xlsx android-artifacts/security-reports/ || true
        cp reports/security/android-security-violations.txt android-artifacts/security-reports/ || true
        
        ls -R android-artifacts

    - name: Upload Android Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: android-app-build-and-reports
        path: android-artifacts/
        if-no-files-found: warn
