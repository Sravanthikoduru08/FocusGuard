<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:padding="24dp">

    <!-- Glowing icon area -->
    <FrameLayout
        android:id="@+id/alert_icon_container"
        android:layout_width="120dp"
        android:layout_height="120dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp">

        <View
            android:id="@+id/block_glow"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/brain_glow_red"
            android:alpha="0.4"/>

        <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:src="@android:drawable/ic_dialog_alert"
            app:tint="@color/white" />
    </FrameLayout>

    <TextView
        android:id="@+id/tv_block_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Attention Instability\nDetected"
        android:textColor="@color/white"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/alert_icon_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <TextView
        android:id="@+id/tv_block_message"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="Continuous passive stimulation detected."
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_block_title" />

    <!-- Emotional Anchor Area -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cv_emotional_anchor"
        android:layout_width="200dp"
        android:layout_height="120dp"
        app:cardCornerRadius="12dp"
        app:cardBackgroundColor="@color/card_dark"
        app:layout_constraintTop_toBottomOf="@id/tv_block_message"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp">

        <ImageView
            android:id="@+id/iv_emotional_anchor"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"/>
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/tv_emotional_quote"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text='"I build for my family&apos;s better tomorrow."'
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="italic"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cv_emotional_anchor" />

    <Button
        android:id="@+id/btn_back_to_focus"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginBottom="16dp"
        android:backgroundTint="#0066FF"
        android:text="Return To Focus"
        android:textAllCaps="false"
        app:cornerRadius="28dp"
        app:layout_constraintBottom_toTopOf="@+id/btn_continue_anyway" />

    <Button
        android:id="@+id/btn_continue_anyway"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginBottom="16dp"
        android:backgroundTint="@color/card_dark"
        android:text="Continue Anyway"
        android:textAllCaps="false"
        android:textColor="@color/text_secondary"
        app:cornerRadius="28dp"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- We keep override UI hidden or removed. The sketch doesn't show text field -->
    <EditText
        android:id="@+id/et_override_reason"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone" />

</androidx.constraintlayout.widget.ConstraintLayout>
